#include <iostream>
#include <conio.h>
#include <cmath>

using namespace std;

int main()
{
	int count;
	
	cout << "Counting Starts Now!" << endl;
	for (count = 1; count <= 10; count++)
		cout << count << "," << " ";
	
	for (count = 12; count <= 30; (count++ + count++))
		cout << count << "," << " ";
		
		
	_getch();
	return 0;
}
